<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apontapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Spending.SpendingActivity"></activity>
        <activity android:name=".ResetPassword.ResetPasswordActivity" />
        <activity
            android:name=".ProductsByList.ProductByListActivity"
            android:label="@string/title_activity_product_by_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditList.EditListActivity" />
        <activity
            android:name=".Products.ProductsActivity"
            android:label="@string/title_activity_products"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewProduct.NewProductActivity" />
        <activity android:name=".NewList.NewListActivity" />
        <activity android:name=".Regist.RegistActivity" />
        <activity
            android:name=".Home.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>